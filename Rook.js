class Rook {
	constructor(startX, startY, Colour) {
		this.X = startX;
		this.Y = startY;
		this.Colour = Colour;

		this.Symbol = "Ro";
	}
	checkLegalMove(desiredX, desiredY) {}
	move() {
        
    }
}
